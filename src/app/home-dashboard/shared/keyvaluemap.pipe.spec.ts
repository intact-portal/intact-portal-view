import { KeyvaluemapPipe } from './keyvaluemap.pipe';

describe('KeyvaluemapPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyvaluemapPipe();
    expect(pipe).toBeTruthy();
  });
});
